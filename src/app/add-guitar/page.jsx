import Form from "@/components/Form/Form";
import React from "react";

const page = () => {
  /*Initialisation du state */
  // const [formDetails, setFormDetails] = useState(formInitialDetails);

  return (
    <section className="">
      <h1> Share your best guitar</h1>
      <Form />
    </section>
  );
};

export default page;
